export interface VideoDetails {
	title: string;
	description: string;
	viewCount: string;
	likeCount: string;
	thumbnail: string;
	commentCount: string;
}

export interface Comment {
	author: string;
	comment: string;
}
